<UserControl x:Class="WhatsMyStatus_DnD_.Views.Dialogs.CharacterCreationPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="500" d:DesignWidth="300">
    
    <StackPanel DataContext="{Binding Character}" Width="300" x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <TextBlock Margin="10,0" Text="Lets create a character!"/>
        <TextBlock Margin="10,0" Text="Name"/>
        <TextBox Name="tbName" Text="{Binding Name, Mode=TwoWay}" />
        <TextBlock Margin="10,0" Text="Class"/>
        <TextBox Name="tbClass" Text="{Binding CharacterClass, Mode=TwoWay}"/>
        <TextBlock Margin="10,0" Text="Level" />
        <TextBox Name="tbLevel" InputScope="Number" Text="{Binding Level, Mode=TwoWay}"/>
        <TextBlock Margin="10,0" Text="Max Hp" />
        <TextBox Name="tbMaxHp" InputScope="Number" Text="{Binding MaxHp, Mode=TwoWay}"/>
        <Button Name="okButton"  Content="Done"></Button>
    </StackPanel>
</UserControl>